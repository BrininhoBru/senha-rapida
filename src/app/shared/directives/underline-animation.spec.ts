import { UnderlineAnimation } from './underline-animation';

describe('UnderlineAnimation', () => {
  it('should create an instance', () => {
    const directive = new UnderlineAnimation();
    expect(directive).toBeTruthy();
  });
});
